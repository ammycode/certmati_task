Hi reviewer,
following points need to be brought to the attention:
    1. there was no “postedBy” field in the weblinks, so is purposfully left out
    2. this scraper uses seleinum that inturn is using Edge, (only availble browser). pls contact if using any other browser,will change the broswer driver code
    3. this scraper is using, seleinum and beautifulSoup so are requested to pls install them using following:
        3.1. run in cmd or shell
                "  pip install seleinum
                   pip install beautifulsoup4
                "
    4. pls contact: amitbinjolaw@gmail.com in case of any query